# Remotion promo

The 15-second repository promo is generated from source, not edited by hand.

```bash
npm install
npm run render
npm run still
```

Output lands in the repository-level `assets/` directory.
