{
  "id": "model-orchestrator",
  "name": "Model Orchestrator",
  "version": "0.3.0",
  "description": "Routes each prompt into fast/daily/deep/critical effort lanes; optionally enforces a model per lane.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enforce": { "type": "boolean", "default": false },
      "router": { "type": "string" },
      "models": { "type": "object", "additionalProperties": { "type": "string" } }
    }
  },
  "uiHints": {
    "enforce": { "label": "Enforce lane model", "help": "Override provider/model per lane instead of only advising." },
    "router": { "label": "Router path", "help": "Path to model_orchestrator.py; defaults to ~/.config/model-orchestrator/model_orchestrator.py." },
    "models": { "label": "Lane models", "help": "Map lane → provider/model, e.g. critical → anthropic/claude-opus-5." }
  }
}
