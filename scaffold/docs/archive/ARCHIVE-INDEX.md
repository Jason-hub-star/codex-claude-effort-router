# Archive index

Archived files keep their history in the name: `<name>-<reason>-<YYYY-MM-DD>.<ext>` where reason is `superseded`, `abandoned`, `legacy`, or `progresslog`.

| File | Why | Replaced by |
|---|---|---|
