# todo

A tiny task list. Tasks are stored in `tasks.json`; you will recieve a JSON export per task with `export`.

Commands: `python -m todo.cli add "title"`, `python -m todo.cli list`, `python -m todo.cli delete-all`.

Run tests with `python3 -m unittest discover -s tests -t .`
