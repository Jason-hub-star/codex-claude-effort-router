# 결정 요약 — 라운드 1 종료 2026-09-07 (답변란 전부 공란 = 권장안 채택, 미결 0)

| # | 결정 | 근거 | 되돌리기 비용 |
|---|---|---|---|
| 01 | 레포명 `effort-lanes`로 지금 변경 | 스타 0·커밋 1, npm/마켓 이름은 리다이렉트 없음 | 높음(공개 후) |
| 02 | 비공개 노하우는 원칙·게이트만 익명화해 공개 | 개인 경로·페르소나·로그 원문 제외 | 높음(공개 후 회수 불가) |
| 03 | 플러그인은 얇은 JS, 분류기는 Python 정본 1곳 | stdlib 배지·drift 방지 | 중 |
| 04 | 강제는 opt-in, 기본 조언 | 캐시·놀람 방지 | 낮음 |
| 05 | 모호 기본 lane DAILY + 프로젝트 설정 floor | `effort_router.py:72` | 낮음 |
| 06 | 스킬 3종 추가(decision-sheet·absorb·harness-audit) → 10종 | 상한 18 실측 | 낮음 |
| 06b | 그릴미 공개명 `decision-sheet`, grill-me 원조 명기 | 이름 충돌 회피 | 중 |
| 07 | 문서 게이트는 실패하는 스크립트 + `--scaffold` | 규약만으론 안 지켜진 실측 | 중 |
| 08 | v0.3 채널: Claude 플러그인 매니페스트·OpenCode npm·npx skills | 상위 3개 공통점 | 낮음 |
| 09 | Hermes는 로컬 실행 검증 후에만 "지원" 표기 | 검증 성역 | 낮음 |
| 10 | 키워드 분류 유지 + 설정 파일 확장 | 결정적·2초 타임아웃 | 낮음 |
| 11 | v0.2.0 태그 → v0.3.0 → 증거 후 v1.0 | | 낮음 |
| 12 | 첫 관문: 공개 30일 100스타 | | 낮음 |
| 13~16 | 확인만 4건 승인(v0.2.0 선커밋·OpenCode 스킬 무설치·OpenClaw metadata 한 줄·로컬 훅 중복 정리) | | |

# GRILL — 풀 하네스 전환  · 라운드 1
> 권장안이 맞으면 답변란을 비워두세요. 다를 때만 적으면 됩니다. 모르겠으면 `?`.
> 다른 안을 고르실 땐 [x]를 **옮겨**주세요(추가 말고 이동) — 두 칸이 체크되면 미결로 처리합니다.
> 정렬: 되돌리기 비싼 순. 작성 2026-09-07. 전제: 포지셔닝 = 풀 하네스(주인님 확정).

## R1-01 · 레포 이름  [상위결정]
Q. `codex-claude-effort-router`라는 이름은 두 런타임만 가리킨다. 5런타임 하네스로 가면 언제 바꾸나?
- [x] **지금** 바꾼다 — 스타 0·커밋 1인 지금이 평생 가장 싼 순간. GitHub가 옛 URL을 리다이렉트하지만 npm·마켓플레이스 이름은 리다이렉트가 없으므로 배포 전에 끝내야 한다. 후보: `effort-lanes` (4-lane이 브랜드) ← 권장
- [ ] 이름 유지 — 이미 링크된 곳 0곳이라 얻는 게 없다
- [ ] 다른 이름: ________
답변:

## R1-02 · 비공개 노하우의 공개 범위  [상위결정]
Q. `jason-agent-harness-template`(private)의 내용 중 무엇을 이 공개 레포로 옮기나?
- [x] **원칙과 게이트만** — 문서 폴더 게이트·Doc Sync Matrix·스킬 18개 상한·"경고→게이트 승격"·"빈 결과≠통과" 같은 규칙과 스크립트. 개인 프로젝트명·경로·"주인님" 페르소나·세션 로그 원문·실측 수치의 출처 레포명은 제거하고 "one operator's measurement, 14 repos, 2026-08"처럼 익명화 ← 권장
- [ ] 스킬만 옮기고 문서·게이트 노하우는 비공개 유지
- [ ] 전부 공개(레포명·수치 원문 포함)
답변:

## R1-03 · 플러그인 언어와 "정본 1곳"  [상위결정]
Q. OpenCode·OpenClaw 플러그인은 프로세스 안에서 도는 TS/JS만 된다(`~/.config/opencode/plugins/*.ts`, `openclaw.plugin.json`). 분류기(현재 Python 140줄)를 어떻게 공유하나?
- [x] **얇은 JS 플러그인이 `python3 effort_router.py --classify`를 호출** — 분류기 정본은 Python 하나, 플러그인은 20~40줄 어댑터. `stdlib only` 배지 유지. 비용: 프롬프트마다 프로세스 1회(~30ms 추정, 실측 필요) ← 권장
- [ ] JS로 이식해 두 구현 유지 — `tests/cases.json`을 두 구현의 공통 계약 테스트로 강제. 비용: 키워드 추가 때마다 두 곳 수정
- [ ] 분류기를 JS로 옮기고 Python 폐기 — Claude/Codex 셸 훅도 node로. 비용: node 없는 호스트 탈락
답변:

## R1-04 · 강제 기본값  [상위결정]
Q. OpenCode(`chat.params.options`)와 OpenClaw(`before_model_resolve`)는 훅이 **실제로** 모델·추론강도를 바꿀 수 있다. 기본으로 켜나?
- [x] **기본 조언(advisory), `enforce: true`는 opt-in** — 세션 중 모델이 저절로 바뀌면 프롬프트 캐시가 깨지고 사용자가 놀란다. 강제는 명시 설정에서만. 대신 README 첫 화면에 "켜면 진짜 바뀐다"를 GIF로 증명 ← 권장
- [ ] 강제 가능한 런타임은 기본 강제 — 차별점을 기본값으로
답변:

## R1-05 · 모호한 프롬프트의 기본 lane  [상위결정]
Q. 이 세션 실측: 같은 프롬프트("ㄱ")에 비공개 라우터=DEEP, 공개 라우터=DAILY. 공개판 기본값은?
- [x] **DAILY 유지** + 프로젝트별 `.effort-router.json`으로 floor를 올릴 수 있게(비공개판의 `RISK_REPOS` 기능을 설정 파일로 일반화) ← 권장. 근거: `router/effort_router.py:72` 기본 daily, 비공개판은 `RISK_REPOS` 하드코딩
- [ ] DEEP 기본 — 비공개판과 동일
답변:

## R1-06 · 추가 스킬 목록  [상위결정]
Q. 현재 7종(아침·조준·수렴·골·페이즈루프·감사·starter). 무엇을 더 넣나? 상한 18(실측 회수율 근거).
- [x] **3종 추가 → 10종**: `decision-sheet`(그릴미: 권장답 채운 질문 파일), `absorb`(흡수: 외부 소스 3분류), `harness-audit`(정비: 회수율 감사, 스크립트 포함) ← 권장. 제외: 랄프루프(Ralph loop는 이미 타 레포 유명작, 차별 없음)·야근(골+페이즈루프 조합으로 대체 가능)
- [ ] 5종 추가(랄프루프·야근 포함) → 12종
- [ ] 추가 없음, 문서·게이트만
답변:

## R1-06b · 그릴미의 공개 이름  [상위결정]
Q. `grill-me`는 Matt Pocock 원조 스킬 이름이라 못 쓴다(주인님 지적). 우리 것의 차이는 "대화로 하나씩"이 아니라 **답을 미리 채운 시트를 파일로 내고 고칠 것만 고친다**는 점. 이름은?
- [x] **`decision-sheet`** — 산출물(결정 시트)이 곧 이름. 기존 패턴(`goal-contract`·`morning-brief`)과 같은 명사형 ← 권장. SKILL.md 본문에 "grill-me의 파일 왕복 변형, 원조 링크"를 한 줄로 밝혀 표절 오해를 막는다
- [ ] `prefilled-interview` — 방식이 이름. 동사형 패턴(`aim-before-build`)과 어울림
- [ ] `answer-first` — 짧지만 뭘 하는지 안 보임
- [ ] 다른 이름: ________
답변:

## R1-07 · 문서·게이트 노하우의 형태  [상위결정]
Q. 문서 폴더 게이트·Doc Sync Matrix·스킬 상한을 "설명"으로 넣나 "실패하는 스크립트"로 넣나?
- [x] **실행 가능한 게이트 + 스캐폴드** — `install.sh --scaffold`가 `docs/{ref,status,evidence,research,archive}` 골격과 `scripts/check-docs.sh`(루트 2파일 규칙·보관 이름 규칙·매트릭스 존재)를 심는다. 근거: 주인님 실측 "규약만 적어두면 하루 만에 루트에 HTML 5개 쌓였다" ← 권장
- [ ] 문서 설명만 — 가볍지만 안 지켜진다
답변:

## R1-08 · 배포 채널 (v0.3 범위)  [상위결정]
Q. 스타 상위 3개의 공통점은 마켓플레이스 등재. 어디까지 v0.3에 넣나?
- [x] **Claude Code 플러그인 매니페스트(`.claude-plugin/`) + OpenCode 플러그인 npm 패키지 + 기존 `npx skills`** — 3개. ClawHub·Hermes 인덱스·Codex 플러그인은 v0.4 ← 권장
- [ ] 6채널 전부 v0.3
- [ ] 마켓플레이스 없이 `install.sh`만
답변:

## R1-09 · Hermes 지원 수준  [상위결정]
Q. Hermes는 조언 주입만 가능(모델·강도 변경 이슈 #23739·#7273 미해결)하고 로컬에 바이너리가 없다.
- [x] **v0.3에 Python 플러그인 포함하되, 로컬에 Hermes를 설치해 실행 검증한 뒤에만 README에 "지원" 표기** — 검증 없이 지원이라 쓰지 않는다(검증 성역) ← 권장
- [ ] v0.4로 미룸
- [ ] 미검증 상태로 "experimental" 표기해 포함
답변:

## R1-10 · 분류 방식  [상위결정]
Q. 키워드 매칭(결정적, LLM 없음)을 유지하나?
- [x] **유지** + 설정 파일로 키워드·floor 확장 — 결정적이라 테스트 가능하고 훅 2초 타임아웃 안에 든다 ← 권장
- [ ] `small_model`로 LLM 분류 추가(선택 모드) — 정확도↑, 지연·비용·비결정성↑
답변:

## R1-11 · 버전 전략  [하위]
Q. 다음 릴리스 번호?
- [x] 지금 미커밋분을 **v0.2.0**으로 먼저 태그, 풀 하네스를 **v0.3.0**, 5런타임 실행 증거가 모이면 **v1.0** ← 권장
- [ ] 바로 v1.0
답변:

## R1-12 · 첫 관문 KPI  [하위]
Q. "10만 스타"는 방향이다. 첫 측정 가능한 관문은?
- [x] **v0.3 공개 후 30일 100스타** — OpenCode 실제 강도 전환 GIF를 앞세워 HN·Reddit r/ClaudeAI·X에 1회 런치. 미달이면 원인을 실측(트래픽 vs 전환)하고 다음 관문 재설정 ← 권장
- [ ] KPI 없이 계속 만든다
답변:

## R1-13 · 확인만  [코드에서 답 나옴]
미커밋 v0.2.0 작업(8파일 수정·5신규)이 `bash scripts/check.sh` **PASS**(방금 실행). 답 확정 시 내가 먼저 커밋·태그한다.
맞으면 넘어가고, 바꿀 거면 여기 적어주세요:

## R1-14 · 확인만  [코드에서 답 나옴]
OpenCode는 `~/.claude/skills`·`~/.agents/skills`를 **그대로 읽는다**(공식 문서). 스킬은 추가 설치 없이 이미 OpenCode에 보인다 — 설치기는 OpenCode용 스킬 복사를 하지 않고 문서에만 적는다.
맞으면 넘어가고, 바꿀 거면 여기 적어주세요:

## R1-15 · 확인만  [코드에서 답 나옴]
OpenClaw 스킬 파서는 frontmatter `metadata`를 **한 줄 JSON만** 허용하고 심링크 디렉토리를 거부한다. 현재 7종은 `metadata` 없음 → 호환. `scripts/check.sh`에 "metadata 있으면 한 줄" 규칙을 추가한다.
맞으면 넘어가고, 바꿀 거면 여기 적어주세요:

## R1-16 · 확인만  [주인님 환경]
`~/.claude/settings.json`에 비공개 `jason-task-router.py`와 공개 `effort-router.py` 훅이 **둘 다** 걸려 있어 매 프롬프트에 라우팅 문맥이 2번 들어간다(이 세션 로그로 확인). 레포 문제가 아니라 도그푸딩 중복. 답 확정 시 공개판 하나만 남기도록 정리해도 되는지:
맞으면 넘어가고, 바꿀 거면 여기 적어주세요:
